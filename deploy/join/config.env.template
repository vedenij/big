# Big Node Configuration - Delegation Server Only
# This node provides GPU computation for small nodes during PoC benchmark phase
# It does NOT participate in blockchain consensus

# HuggingFace cache location (for ML models)
export HF_HOME=/mnt/shared

# Delegation Server Configuration
export DELEGATION_PORT=9090
export DELEGATION_MAX_SESSIONS=10
export DELEGATION_AUTH_TOKEN=your-secret-token-change-this
